server:
  servlet:
    context-path: /api
spring:
  #  datasource:
  #    url: jdbc:postgresql://localhost:5432/cloudlab-db
  #    password: postgres
  #    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org.hibernate.engine.jdbc.env.internal.LobCreatorBuilderImpl: ERROR
